classDiagram
    class Owner {
        -name: str
        -available_hours_per_day: float
        -preferred_activity_times: list
        -preferences: dict
        -pets: list
        +add_pet(pet) void
        +get_all_tasks() list
        +filter_tasks(pet_name, completion_status) list
        +get_available_time() float
        +get_preference(key) any
        +set_preference(key, value) void
    }

    class Pet {
        -name: str
        -species: str
        -age: int
        -special_needs: list
        -care_requirements: dict
        -tasks: list
        +get_required_tasks() list
        +add_task(task) void
        +mark_task_complete(task_title) bool
        +add_special_need(need) void
        +get_health_info() str
    }

    class Task {
        -title: str
        -description: str
        -duration_minutes: int
        -priority: str
        -task_type: str
        -frequency: str
        -required_time: str
        -completion_status: bool
        -dependencies: list
        -task_date: datetime
        +get_duration() int
        +is_urgent() bool
        +mark_complete() void
    }

    class Schedule {
        -owner: Owner
        -pet: Pet
        -tasks: list
        -daily_plan: list
        -start_time: str
        -end_time: str
        +add_task(task) void
        +sort_by_time() list
        -_sort_tasks_by_priority() list
        +generate_schedule() list
        +can_schedule_at(task, time) bool
        +get_plan_with_reasoning() str
        +detect_conflicts(buffer_minutes) dict
        +get_conflict_warnings(buffer_minutes) list
        +detect_cross_pet_conflicts(buffer_minutes) dict
        +get_cross_pet_warnings(buffer_minutes) list
    }

    Owner "1" --> "*" Pet: has
    Pet "1" --> "*" Task: requires
    Schedule --> Owner: uses constraints
    Schedule --> Pet: schedules for
    Schedule --> Task: arranges